

/*
var canvas = document.getElementById('drawContainer');
var context = canvas.getContext('2d');

context.beginPath();

// Example line that is repeated in the loop
context.moveTo(300, 200);      // Bottom left
context.lineTo(550, 50);      // Up
context.fill();
context.closepath();
*/

/*
function draw_line(start_x, start_y, to_x, to_y) {

    var c = document.getElementById("drawContainer");
    var context = c.getContext("2d");

    ctx.fillStyle = "green";
    ctx.fillRect(0, 0, myCanvas.width, myCanvas.height);
    ctx.fillStyle = "red";
    ctx.fillRect(50, 50, 100, 100);

    context.moveTo(start_x, start_y);
    context.lineTo(to_x, to_y);
    context.stroke();

}*/








